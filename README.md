Basic Chess app (created using React and Node)

on top of rendering basic chessboard it also has built in chess engine (stockfish) for 
1. rendering eval bar 
2. making moves when playing against computer